#include <stdio.h>

int main()
{
int a;
long int b;
printf("enter the inital sum of more than 10000:");
scanf("%li",&b);
printf("\n");

printf("enter the number of days less than a year:");
scanf("%d",&a);
printf("\n");

return (0);
}
